import { sanitize } from '@laeri/config-parser'
import { Config } from './generated/config.interface'
import { parse } from './generated/parser'

export const createConfig = (): Config => sanitize(parse)
